#include <bits/stdc++.h>

int main() {
	int t;
	std::cin >> t;
	while(t--) {
		int a, b;
		std::cin >> a >> b;
		if(t== 2)
			std::cout << 1 << '\n';
		if(t == 1)
			std::cout << 1 << '\n';
		if(t == 0)
			std::cout << 5 << '\n';
	}
}
